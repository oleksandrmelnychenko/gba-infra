# DEV bug release — 2026-09-16

Status: API and Console deployed and verified healthy. Analytics was built and
published for solution coverage, but its runtime was not changed because none
of the five bug paths execute in that service.

## Source and immutable runtime

- Server source: `77cb104bd0197e2a404770177ef4403f9c7ec785` from `origin/main`.
- Console source: `f29a3f62d3c3f9a5356752a0e43b5543d6f789dc` from `origin/main`.
- API image: `gba-data-concord:bugs-1244-1253-1254-1257-1260-77cb104`
  (`sha256:1b3811ea62aca1fabecc23d46bca5390cf6ac6a86da854dc97f537cf013e2ec0`).
- Console image: `gba-console:bugs-1244-1253-1254-1257-1260-f29a3f6`
  (`sha256:85fc6836feb0bb469e819c1d79edc4296515931b3bf2040d65a4f59a7f8bc609`).
- API assembly SHA-256 in the image and host Release publish:
  `d6e5dfa46fd38a8600fc990a7ad30a5aba24408415258ab9b1ae924df09b6ff7`.
- Console `build.json` SHA-256 in the image and host staging output:
  `fa877d67db793996ac2b4f10635b80d94caf557fb7fdd139c79dbd5722b1dede`.

The host BuildKit content store was unable to complete a normal layer build.
The API image was therefore assembled as an OCI child image over the existing
verified runtime base, with an opaque `/app` layer containing the exact Release
publish. Its entrypoint, `APP_DLL`, revision labels and DLL digest were checked
before deployment. The reproducible API OCI archive is retained at
`/root/projects/gba-release-worktrees/image-assembly-77cb/gba-data-concord-77cb104.tar`
with SHA-256
`2884ee1b28fe71f7b467c858a06f9d3331b0c59058c3ece1d348888ba163449f`.

## Verification

- BUG-1244 live read-only SQL audit: 4/4 passed, covering both 1C worlds,
  the complete AYMEKS and SAMPIYON matrices, expenses, quantities, storage
  split, duplicate resistance, and nonpositive source rows.
- Console focused suite: 72/72 passed; production build and full ESLint passed;
  React Doctor reported 100/100.
- API focused suite: 38/38 passed. Platform actor/repository suite: 106/106
  passed. The shop-read logging regression suite additionally passed 8/8.
- BUG-1253 live registry: `КСн00002860` exposes both receipt and accounting
  payment fields. SQL confirms its accounting amount is correctly zero because
  the sale currently has no income-payment link.
- BUG-1254 live database coverage: one retail sale has two active income links;
  accounting paid and linked income-order totals both equal `5623.88`.
- BUG-1257 live API: `КСн00002864` returned three sale items; the selected line
  had unit price `335.1504`; sale-details and product-card endpoints both
  returned HTTP 200 without serialization failure.
- BUG-1260 authenticated UI: roles returned HTTP 200, password fields use
  `new-password`, email autocomplete is disabled on the new-user form, and a
  blank submission issued zero create-user requests. Actor tests cover success,
  all downstream failures, and two-sided compensation.

After deployment, API and Console are healthy with zero restarts, API `/health`
and Console `/` return HTTP 200, and authenticated smoke tests pass against the
deployed Console image.

## Deployment and rollback

Append `dev-bugs-1244-1253-1254-1257-1260-20260916.compose.yml` after the
existing DEV compose chain and recreate only `data-concord` and `gba-console`
with `--no-deps --no-build`.

Pre-release runtime pins retained for rollback:

- API: `gba-data-concord:reports-price-type-60f3484fc`
- Console: `gba-console:reports-price-type-03035c7b`

No database migration, business-data mutation, 1C synchronization, Analytics
runtime recreation, or PROD deployment was performed by this release.
