# Append to each service's active DEV compose files, then recreate only that service.
# Existing AI fleet, native cost coverage and other service overlays stay in place.
# No new database migration. Requires the prior ReportUserTemplates migration.
services:
  data-analytics:
    # gba-server 54d351ec7: native datasets, exact decimals and imported return currency proof.
    image: gba-data-analytics:report-native-wave2-54d351ec
  gba-console:
    # gba_console 0f0bbf09: native datasets, merged XLSX metadata, charts and external-port URLs.
    image: gba-console:report-native-wave2-0f0bbf09
