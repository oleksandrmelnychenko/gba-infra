# DEV: каталог звітів, приватні шаблони й точний договір клієнта

Реліз додає до наявного конструктора каталог усіх підтверджених звітних об'єктів Fenix/AMG, приватне збереження шаблонів у GBA SQL та швидкі налаштування за договорами. Відбір і групування використовують конкретний ClientAgreement Id. Старий відбір тільки з AgreementId отримує 400 і пояснення повторного вибору.

Каталог 377 позицій є інвентаризацією. Повне перенесення всіх розрахунків і числова відповідність 1С не завершені.

## Розгорнуті образи

| Сервіс | Образ | Код runtime | Image ID |
|---|---|---|---|
| data-analytics | gba-data-analytics:report-workspace-20260907-v2 | 16eec91f9 | sha256:d8ef222c5d3eeeafbc0e032383509916defb3377f2bca2071aa0eea9864798ef |
| gba-console | gba-console:main-ef544985 | ef544985 | sha256:2afac484aebee96fd4c757421afbbb52015d43a618c2f08d2c12dd6b186fa66e |

Console ef544985 включає зміни звітів c769c07f і вже опубліковане оновлення залежностей. Активні Compose overlays інших релізів збережені; перевідтворені тільки цільові сервіси. Обидва healthy. Production не змінювався.

## Схема та перевірки

- Міграція `20260907180000_AddReportUserTemplates` застосована до GBA DEV `ConcordDb_V5`: приватна таблиця шаблонів, ключ власник/id, ревізії та JSON definition. Жодних змін 1С.
- 36 тестів SQL/договорів/шаблонів із реальними тимчасовими GBA SQL базами; 21 перевірка workbook і меж нативних джерел; 82 тести конструктора консолі. React Doctor 91/100, без помилок.
- Браузер: каталог 377; фільтр регламентованих 58 / Fenix 57; створення шаблону, перезавантаження, відновлення періоду й договору, видалення лише тестового шаблону.
- API без сесії 401, без права звітів 403. Після фінального оновлення каталог/шаблони 200; неоднозначний legacy відбір договору 400 з точним поясненням.

Evidence: `/root/evidence/report-port-2026-09-07/REPORT.md`, `deployment-final.json`, `migration-receipt.json`, `browser-published-console-verification.log`, `live-contract-validation.log`. План SQL-перенесення: `/root/projects/gba-server/docs/report-port-20260907.md`. Початкове читання 1С виключно SELECT і інертний розбір файлів; BSL/звіти/налаштування джерел не виконувались.

Для повторного застосування додати `dev-report-workspace-20260907.compose.yml` після активних overlays сервісу та виконати `up -d --no-deps --no-build` із конкретною назвою сервісу. Перевірити новіші опубліковані образи перед повторним застосуванням історичного релізу. Адитивну таблицю шаблонів при поверненні попереднього образу зберігати, щоб не втратити користувацькі налаштування.
