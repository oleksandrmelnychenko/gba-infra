# environment files — real values, never commit
.env
.env.dev
.env.prod

# Docker secret files — real values, never commit
secrets/dev/
secrets/prod/

# Caddy runtime data
caddy/data/
caddy/config/

# Python tooling
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
