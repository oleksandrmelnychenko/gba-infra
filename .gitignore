# environment files — real values, never commit
.env
.env.dev
.env.e2e
.env.prod

# Docker secret files — real values, never commit
secrets/dev/
secrets/e2e/
secrets/prod/

# Caddy runtime data
caddy/data/
caddy/config/

# Python tooling
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
